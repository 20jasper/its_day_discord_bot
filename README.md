# it's day discord bot
A bot to change the day of the week in a discord channel

## secrets
Example secrets are stored in `secrets.example.toml`, though these secrets must be manually added to the environment